"""Streamlit アプリのエントリポイント（Step 4 以降で実装）"""

import streamlit as st

st.set_page_config(page_title="高校数学答案診断", layout="wide")
st.title("高校数学答案 OCR・診断LLMパイプライン")
st.info("Step 4 以降で各ページを実装予定です。現時点では `python -m src smoke` と `pytest` で動作確認してください。")
